```xml
<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================
         NETWORK
         ============================================================ -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ============================================================
         BIOMETRIC AUTHENTICATION
         ============================================================ -->

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
        Keep storage permissions out unless LoanX genuinely requires them.

        Android's scoped-storage model makes the old broad-storage permissions
        unnecessary for most modern applications.

        <uses-permission
            android:name="android.permission.READ_EXTERNAL_STORAGE"
            android:maxSdkVersion="32" />

        <uses-permission
            android:name="android.permission.WRITE_EXTERNAL_STORAGE"
            android:maxSdkVersion="28" />

        <uses-permission
            android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
            tools:ignore="ScopedStorage" />
    -->

    <!-- ============================================================
         APPLICATION
         ============================================================ -->

    <application
        android:name="${applicationName}"
        android:label="Loanx"
        android:icon="@mipmap/ic_launcher"

        android:allowBackup="false"
        android:fullBackupContent="false"

        android:requestLegacyExternalStorage="true"

        android:enableOnBackInvokedCallback="true">

        <!-- ========================================================
             MAIN FLUTTER ACTIVITY
             ========================================================

             IMPORTANT:

             Hardware acceleration intentionally starts disabled here.

             MainActivity.kt determines whether GPU rendering should be
             enabled for the current device before Flutter's content is
             created.

             Android allows hardware acceleration to be enabled
             programmatically, but it cannot be disabled afterward if it
             was enabled through the manifest.
             ======================================================== -->

        <activity
            android:name=".MainActivity"

            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""

            android:theme="@style/LaunchTheme"

            android:hardwareAccelerated="false"

            android:windowSoftInputMode="adjustResize"

            android:configChanges="
                orientation|
                keyboardHidden|
                keyboard|
                screenSize|
                smallestScreenSize|
                locale|
                layoutDirection|
                fontScale|
                screenLayout|
                density|
                uiMode">

            <!-- ====================================================
                 NORMAL FLUTTER THEME
                 ==================================================== -->

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- ====================================================
                 APPLICATION LAUNCHER
                 ==================================================== -->

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <!-- ========================================================
             FLUTTER EMBEDDING V2
             ======================================================== -->

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- ============================================================
         PACKAGE VISIBILITY
         ============================================================

         These declarations allow the application to discover Android
         activities/services capable of handling the relevant intents.

         Flutter's ProcessTextPlugin uses PROCESS_TEXT.

         Custom Tabs / external browser integrations may use
         CustomTabsService.

         HTTPS VIEW allows browser/web-link handlers to be discovered.
         ============================================================ -->

    <queries>

        <!-- HTTPS links / browser handlers -->
        <intent>

            <action android:name="android.intent.action.VIEW" />

            <data android:scheme="https" />

        </intent>

        <!-- Android Custom Tabs -->
        <intent>

            <action
                android:name="android.support.customtabs.action.CustomTabsService" />

        </intent>

        <!-- Flutter text processing integration -->
        <intent>

            <action android:name="android.intent.action.PROCESS_TEXT" />

            <data android:mimeType="text/plain" />

        </intent>

    </queries>

</manifest>
```
